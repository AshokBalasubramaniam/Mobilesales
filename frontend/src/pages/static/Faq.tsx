import FAQSection from "../../components/home/FAQSection";

const classes = {
  wrapper: "pt-6",
};

const Faq = () => (
  <div className={classes.wrapper}>
    <FAQSection />
  </div>
);

export default Faq;
